<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/Black"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.androidchess.Home" >

    <Button
        android:id="@+id/sortByDate"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:textColor="@color/White"
        android:text="@string/new_game"
        android:onClick="newGame" />

    <Button
        android:id="@+id/sortByTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/sortByDate"
        android:layout_below="@+id/sortByDate"
        android:layout_marginTop="18dp"
        android:textColor="@color/White"        
        android:text="@string/view_saved_games"
        android:onClick="viewSavedGames" />
    
   <Button
        android:id="@+id/button3"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/sortByTitle"
        android:layout_below="@+id/sortByTitle"
        android:layout_marginTop="18dp"
        android:textColor="@color/White"        
        android:text="@string/record_games_button"
        android:onClick="recordGames" />

</RelativeLayout>
